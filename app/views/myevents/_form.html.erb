<%= form_with(model: myevent) do |form| %>
  <% if myevent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(myevent.errors.count, "error") %> prohibited this myevent from being saved:</h2>

      <ul>
        <% myevent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :event_date, style: "display: block" %>
    <%= form.date_field :event_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
